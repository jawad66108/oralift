"""
Auto-generated by OraLift scaffold.py from PL/SQL unit: {{ unit.name }}
Source: {{ unit.source_file }} (lines {{ unit.start_line }}-{{ unit.end_line }})
"""

import pytest
from fastapi.testclient import TestClient


def test_{{ unit.name | lower }}_returns_not_implemented(client: TestClient):
    """
    Placeholder test -- the generated route currently returns 501.
    Replace this once the real implementation lands behind the route.
    """
    payload = {
{% for p in request_params %}
        "{{ p.name.lstrip('p_') }}": None,  # TODO: supply a realistic {{ p.datatype }} value
{% endfor %}
    }
    response = client.post("/{{ unit.name | lower }}", json=payload)
    assert response.status_code == 501
